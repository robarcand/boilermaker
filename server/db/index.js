const db = require('./database')

// write out relationships between our tables here ( i.e. Robot.belongsToMany(Project, {through: 'robotProject'}) )

module.exports = {
    db,
    // be sure to export your tables here
}